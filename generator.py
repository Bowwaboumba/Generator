import string
import random
begin = input("GENERATOR is a random numbers generator and sequence of letters. Change randint to change it's capacity; Press Enter. ")
print(random.randint(1, 100))
print(string.ascii_letters)

input('Appuyez sur entrée ...')

n = (random.randint(1, 100))
